"""Tactile cross-attention conditioned flow matching decoder.

Import concrete symbols from submodules (e.g. ``train_frs.utils.model``)
so spawn data-loader workers can import ``window_io`` without pulling JAX/CUDA.
"""
