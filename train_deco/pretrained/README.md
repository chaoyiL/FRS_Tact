# DECO backbone artifact

Place the ResNet34 initialization weight here as:

```text
resnet34-b627a593
```

On the current workstation the source copy is:

```text
/home/hillbot/deco/deco_baseline/pretrained/resnet34-b627a593
```

The binary is intentionally not committed. Set `BACKBONE_WEIGHTS` to use another path.
