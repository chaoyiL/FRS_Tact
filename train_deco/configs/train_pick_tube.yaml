# 训练合同参考；正式参数由 scripts/train.sh 传给 train_deco.train。
dataset:
  format: lerobot-v21
  camera_names:
    - observation.images.camera0
    - observation.images.camera1
  state_dim: 20
  action_dim: 20
  expected_sample_hz: 30.0
model:
  action_chunk_size: 32
  hidden_dim: 512
  layers: 6
  heads: 8
  image_size: 256
  inference_steps: 5
  rope_height: 256
  rope_width: 256
export:
  torchscript_image_height: 224
  torchscript_image_width: 224
