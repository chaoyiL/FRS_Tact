# Pi0.5 + FRS deployment configuration.
checkpoint: /home/typhon/ManiSkill-vitac/checkpoints/8000_pick_tube
seed: 0
num_steps: 10

model:
  action_dim: 20
  action_horizon: 50
  state_dim: 20
  robot_action_dim: 20
  paligemma_variant: gemma_2b_lora
  action_expert_variant: gemma_300m_lora
  camera_map:
    left_wrist_0_rgb: observation.images.camera0
    right_wrist_0_rgb: observation.images.camera1
  empty_cameras: []

norm_stats:
  dir: /home/typhon/ManiSkill-vitac/checkpoints/8000_pick_tube/assets
  asset_id: pick_tube_all
  use_quantile_norm: true

frs:
  enabled: true
  checkpoint: /home/typhon/FRS_Tact/checkpoints/frs/pi05_frs_pick_tube_8000_bimanual_last
  tactile_encoder_checkpoint: /home/typhon/FRS_Tact/checkpoints/encoder/encoder_ckpt_0809
  tactile_keys:
    - observation.images.tactile_left_0
    - observation.images.tactile_right_0
    - observation.images.tactile_left_1
    - observation.images.tactile_right_1
  # Training contract: action_horizon 50 / divisor 5 = tactile window 10.
  tactile_window_divisor: 5
  history_stride: 3
  reverse_steps: 50
  reverse_solver: slerpflow
  decode_steps: 10
  decode_solver: fireflow
  steering_protection_interval_s: null
  temporal_ensemble_coeff: 0.1
  # Parsed for legacy checkpoint compatibility, but PI0.5 VLA owns grippers 9/19.
  gripper_gain: null
  verify_source_checkpoint_fingerprint: false
  max_normalized_action_abs: 8.0
  max_normalized_delta_rms: 4.0

connection:
  address: 127.0.0.1
  port: 26421
  add_port: true
  retry_interval_s: 1.0
  ping_interval_s: 20.0
  ping_timeout_s: 20.0
  observation_timeout_s: 30.0
  action_ack_timeout_s: 30.0
  token: null
  token_env: VB_ROBOT_TOKEN
  require_token: true

observation:
  data_type: vitac
  language_prompt: Use the left hand to pick up the green tube, and then use the right hand to pick up the blue tube.
  single_arm_mode: false
  no_state_obs_mode: false

control:
  control_frequency: 10.0
  controller_frequency: 80.0
  action_horizon: 50
  steps_per_inference: 50

runtime:
  auto_start: false
  warmup_runs: 1
  max_iterations: 0

logging:
  save_observations: true
  output_dir: outputs/pi05_frs_observations
  save_every: 1
  queue_size: 32
