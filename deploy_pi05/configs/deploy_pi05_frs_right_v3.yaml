# Right-hand single-arm Pi0.5 + learned-residual Gate FRS deployment.
# This file is intentionally separate from deploy_pi05_frs_right.yaml, which
# remains the objective-v2 flow-decoder deployment.

checkpoint: /home/typhon/FRS_Tact/checkpoints/model/pi05_task3_0830_1w
seed: 0
num_steps: 10
task: 0

model:
  state_action_profile: single-right-arm-7x10
  action_dim: 10
  action_horizon: 50
  state_dim: 7
  robot_action_dim: 10
  paligemma_variant: gemma_2b_lora
  action_expert_variant: gemma_300m_lora
  camera_map:
    right_wrist_0_rgb: observation.images.camera1
  empty_cameras: []

norm_stats:
  dir: /home/typhon/FRS_Tact/checkpoints/model/pi05_task3_0830_1w/assets
  asset_id: insert_0102_train90
  use_quantile_norm: true

frs:
  enabled: true

  # Must be an objective-v3 learned_residual_gated checkpoint. The runtime
  # fails closed if its architecture, arm9/gripper contract, thresholds, or
  # residual bound do not match the checkpoint metadata and decoder config.
  checkpoint: /home/typhon/FRS_Tact/checkpoints/model/pi05_frs_insert_01_right_two_face_learned_residual_v3
  tactile_encoder_checkpoint: /home/typhon/FRS_Tact/checkpoints/encoder/encoder_ckpt_0824

  tactile_keys:
    - observation.images.tactile_left_1
    - observation.images.tactile_right_1

  tactile_window_divisor: 5
  history_stride: 3

  # Retained for config/provenance compatibility with the v1/v2 runtime. The
  # objective-v3 path directly predicts a bounded residual and does not flow-decode.
  reverse_steps: 50
  reverse_solver: slerpflow
  decode_steps: 10
  decode_solver: fireflow

  steering_protection_interval_s: null
  # Preserve the checkpoint's learned chunk Gate exactly.
  temporal_ensemble_coeff: null
  gripper_gain: null

  verify_source_checkpoint_fingerprint: false
  max_normalized_action_abs: 8.0
  max_normalized_delta_rms: 4.0
  # v3 owns its Gate and residual bound in checkpoint metadata/model config.
  hard_low_gate_bypass: false
  max_normalized_residual_abs: null

connection:
  address: 127.0.0.1
  port: 26421
  add_port: true
  retry_interval_s: 1.0
  ping_interval_s: 20.0
  ping_timeout_s: 20.0
  observation_timeout_s: 30.0
  action_ack_timeout_s: 30.0
  token: null
  token_env: VB_ROBOT_TOKEN
  require_token: true

observation:
  data_type: vitac
  language_prompt: Firstly, use the hand to pick up the tube and then insert it back.
  single_arm_mode: true
  controlled_arm: right
  no_state_obs_mode: false

control:
  control_frequency: 10.0
  controller_frequency: 80.0
  action_horizon: 50
  steps_per_inference: 50

gripper:
  hysteresis_enabled: false
  left_close_threshold: 0.08
  left_reopen_threshold: 0.10
  left_closed_command: 0.01
  right_close_threshold: 0.105
  right_reopen_threshold: 0.115
  right_closed_command: 0.01

runtime:
  auto_start: false
  warmup_runs: 1
  status_interval_s: 2.0
  max_iterations: 0

logging:
  save_observations: true
  output_dir: outputs/pi05_frs_right_v3_observations
  save_every: 1
  queue_size: 32
