These two unmodified upstream RDP source files are numerical regression fixtures.
They are copied from the user-provided reactive_diffusion_policy-main tree and
retain its MIT license. Tests prefer that original tree when it is available,
and use these fixtures in clean checkouts. Runtime training imports neither file.
